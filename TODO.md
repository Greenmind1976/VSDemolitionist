# TODO

- Consider ore-block quality preservation/recovery logic so explosive ore recovery can optionally reflect source block quality instead of always yielding poor ore.
- Investigate how dynamite works in water.
- Configure the app to use the ConfigLib mod.
- Look into remote controlled charges specifically for extracting unbroken rock.
- Consider a configurable blast-triggered support-collapse system so destroying base rock with dynamite can bring down unsupported parts of a mountain; keep it scoped to support blocks removed by explosives rather than always-on world collapse.
- Explore configurable UXO functionality so explosives have a chance to fail to detonate when used, remain dangerous in the world, and can still explode if someone tries to pick them up.
- Add a disarm mechanic for landmines and claymores, including whether a crafted disarm stick or probe tool makes sense for safer recovery.
- Add warning to documentation about large blast drop/entity volume causing performance or render issues.
- Allow higher unbroken block stack size.
- Enable support for Config Lib mod with user-facing settings UI.
- Retest water explosion sounds later (above/below branches, distance scaling, and /dynamitesounds toggle behavior).
